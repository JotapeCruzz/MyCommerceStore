name: ecommerce_my_store
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  firebase_auth: ^6.1.2
  firebase_core: ^4.2.1
  flutter:
    sdk: flutter
  flutter_svg: ^2.2.2
  provider: ^6.1.1  
  qr_flutter: ^4.1.0
  google_sign_in: ^6.3.0
  google_identity_services_web: ^0.3.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets: 
    - assets/images/
    - assets/svgs/
